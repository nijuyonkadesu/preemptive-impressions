[self-hostable cloudflare warp socks5 proxy](https://github.com/adasThePrime/cloudflare-warp-proxy)  just clone the repo in local, run docker compose up -d. enable socks5 in compose.yml, and adjust IPs if it were to clash. That's all.

